# Intelligence Positioning, Jacob Transformation Record, and OG Identity Release

## Status

Implementation and local verification complete; immutable deployment candidate and Production promotion remain. The user-authored release brief dated 2026-09-10 is the controlling assignment and explicitly authorizes inspection, design, implementation, verification, commit, push, deployment, and deployed-result verification for this scope.

## Mission

Extend the public Mariner Nexus model from Strategy, Experience, and Systems to Strategy, Experience, Systems, and Intelligence; make that fourth discipline tangible through Jacob Mariner Transformation Record 06; and replace the social-preview experience with a deterministic route-aware Mariner Nexus OG identity system.

## Scope

- Intentionally update full-architecture positioning on `/` and `/capabilities`.
- Reframe the connected-systems model around information, structure, context, intelligence, and approved human action.
- Add Jacob Mariner to the typed public Work registry and publish `/work/jacob-mariner`.
- Build a Jacob-specific evidence-led case-study composition using only current first-party public facts and product captures.
- Distinguish current product behavior from future intelligence architecture in both content and visual semantics.
- Add reusable 1200 × 630 OG identity output for `/`, `/capabilities`, `/work`, and `/work/jacob-mariner`.
- Update metadata, structured data, sitemap, and route-level social identity where supported by the existing architecture.
- Extend the existing responsive Content Integrity and release QA coverage for the new route and compositions.
- Record the approved positioning, publication, architecture, and release decisions.
- Commit, push, deploy through the configured Vercel workflow, and verify the resulting public metadata and routes.

## Non-goals

- No autonomous AI feature is added to the Mariner Nexus application.
- No CMS, database, authentication, CRM, 3D renderer, analytics product, or new runtime dependency.
- No fabricated athlete data, inferred game statistics, synthesized athlete imagery, or unsupported operational claim.
- No navigation, inquiry persistence, or service-scope change beyond the brief's explicit Intelligence authorization.
- No unrelated refactor or redesign of existing Transformation Records.

## Authority and truth

- The current repository is `LEGACY_MODE`; no root `.wds` manifest or normalized project exists.
- Root `AGENTS.md` and `docs/mariner-nexus-redesign/` remain canonical repository doctrine.
- The attached user brief explicitly supersedes DEC-002, DEC-003, and the existing project-publication allowlist only for the described Intelligence/Jacob release. A new decision will record that change without rewriting historical decisions.
- `https://www.jacobmariner.com/` is the first-party factual source. Current rendered values and source classifications override stale examples in the brief.
- Required Input: none currently identified. No credential content may enter the repository or evidence set.

## Implementation obligations

- Server Components and typed repository content remain the default.
- Use CSS/DOM/SVG architecture; preserve native scrolling and reduced-motion behavior.
- Use local, optimized, correctly dimensioned first-party captures with descriptive alt text.
- Preserve the existing authorization derivation for public Work records.
- Keep future-state intelligence features visibly and semantically separate from current capabilities.
- Preserve immediate navigation feedback and avoid new hydration/runtime cost unless an interaction materially requires it.

## Gates and evidence

- Install integrity and dependency audit.
- `npm run typegen`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build`.
- Content Integrity audit expanded to all canonical routes and required widths: 375, 390, 430, 768, 1024, 1280, 1440, and ultra-wide coverage.
- Chromium browser review of `/`, `/capabilities`, `/work`, `/work/jacob-mariner`, every existing Transformation Record, navigation, mobile menu, project/external CTAs, `/start`, 404 behavior, and social metadata.
- Representative Safari review where locally available.
- Keyboard/focus, heading order, alt text, contrast, reduced motion, no-JavaScript fallback, console/runtime errors, horizontal overflow, and visible-text containment.
- Verify OG responses are public `image/png`, 1200 × 630, HTTP 200, and referenced by absolute production-safe metadata URLs.
- Review image weight, hydration/client boundaries, build route output, and material performance implications.
- Preview deployment verification before Production; Production alias movement is permitted by the user brief only after all release gates pass.

## Human review boundary

The brief supplies the strategic and publication authorization required to implement and release. Visual meaning, identity, and factual representation remain reviewable judgments; implementation may proceed, but any unresolved ambiguity that could materially change a claim or public representation must be surfaced instead of invented.

## Implementation record

- Added one shared typed discipline source for Strategy, Experience, Systems, and bounded Intelligence, then applied it to Home, Capabilities, the Nexus, Company language, and the footer without adding a client boundary.
- Published Jacob Mariner as Transformation Record 06 through the existing authorization-derived Work registry. The record carries source classes, source URLs, publication/verification dates, product captures, a current architecture, and a separately labeled future architecture.
- Added one reusable server-rendered OpenGraph identity component and four static 1200 × 630 routes: Home, Capabilities, Work, and Jacob Mariner.
- Replaced the 701,373-byte source logo as the metadata icon with first-party 48 × 48 and 180 × 180 PNG derivatives weighing 2,527 and 20,987 bytes. The larger source remains available only through image optimization where the visible header requires it.
- Upgraded and exact-pinned `next` and `eslint-config-next` from 16.3.2 to the patched 16.3.4 release. The final dependency audit reports zero vulnerabilities.
- Kept the Jacob record server-rendered. Its tablet and mobile proof captures are intentionally eager because exact-width browser testing exposed unreliable lazy loading at the first product-proof boundary; their measured mobile transfer is approximately 28 KB combined.

## Local validation record

- Final optimized build: PASS on Next.js 16.3.4; 28 static pages generated, including six Transformation Records and all four static OpenGraph routes.
- Type generation, TypeScript, ESLint, 12 application tests, and 63 WDS kernel tests: PASS.
- WDS detector: `LEGACY_MODE`. `wds:validate` and `wds:check-generated` are not applicable because this repository intentionally has no root `project.yaml`; no WDS bootstrap was introduced.
- Content Integrity: 278 states audited across 14 canonical routes, thirteen widths from 320 through 1600, Nexus and Range states, open Capabilities architecture, reduced motion, and JavaScript-free records; 278/278 PASS with zero overflow, clipped text, or heading collisions.
- Chromium 153: exact-width Home, Capabilities, Work, and Jacob review; targeted Jacob rerun passed at 1440, 1024, 768, 430, 390, 375, and 320. No console exceptions, bad responses, actionable network failures, or reduced-motion violations. Mobile menu body lock, initial focus, Escape close, and focus restoration passed.
- Safari 26.6.2 at 1080 × 768: Home, Capabilities, Jacob, Work-to-record navigation, Start, four-discipline architecture, Nexus reconfiguration, keyboard focus/activation, and external source destinations passed. Safari console tooling was not separately inspected; Chromium supplied automated console evidence.
- Route probe: all 14 canonical pages, sitemap, robots, and four OpenGraph images return 200; unknown page and unknown OpenGraph card return 404.
- Metadata: exact canonical, title, description, OpenGraph, Twitter, width, height, and alt tags verified for Home, Capabilities, Work, and Jacob. Sitemap lists all 14 canonical URLs. JSON-LD emits Organization, Person, WebSite, Service, ItemList, and CreativeWork without deprecated `ProfessionalService` markup.
- OpenGraph: all four direct responses are public `image/png`, 1200 × 630, and visually inspected.
- Source truth: the live first-party Jacob product and outbound Instagram, Athletic.net, MileSplit, and LuHi evidence were checked on 2026-09-11. Athletic.net blocks scripted curl but loaded normally in Safari with the cited records visible.
- Synthetic local performance: Home mobile at 1.6 Mbps/150 ms/CPU ×4 recorded 1.336 s LCP, CLS 0, and one 76 ms long task; Jacob mobile recorded 1.820 s LCP, CLS 0, and one 107 ms long task. Desktop LCP measured 108 ms Home and 120 ms Jacob with CLS 0 and no long tasks. The Jacob server-rendered record adds approximately 3.9 KB compressed JavaScript over Home on mobile; its larger route delta is four fonts at approximately 145 KB. These are controlled synthetic results, not field data.
- Dependency-tree note: a clean `npm ci` and `npm prune` both complete successfully, while npm 11 still labels Sharp's lockfile-listed optional wasm fallback and its runtime as extraneous. `npm ls --depth=0` exits 0; no application dependency or vulnerability is implicated.

## Deviations and accepted tradeoffs

- The homepage keeps four project worlds by replacing its Sovereign preview with Jacob; Sovereign remains fully published on `/work` and at its direct record. This preserves the approved four-world composition while making the newly authorized proof visible.
- Representative Safari review used one desktop window; the complete mobile/tablet width matrix ran in Chromium. No browser-specific visual or interaction defect was observed.
- No field performance data is available before release. Production verification uses the immutable candidate and public alias, with ongoing observability remaining governed by DEC-045.

## Stop condition

Stop only when the scoped implementation is committed and pushed; a matching Vercel artifact is READY; production routes and metadata are verified if promotion succeeds; rollback remains identified; and the executive release report records validation, performance implications, deviations, deployment identifiers, and legitimate remaining risks.
